/**
 * SUPERSEDED by test/sdd/speedrun-pg-finality.target.test.ts
 *
 * Former characterization of:
 * - AthenaResult without affectedRows
 * - Gateway HTTP ignoring affected_rows aliases
 * - PG compiler rejecting Speedrun `.or("deleted.eq.false,deleted.is.null")`
 *
 * Kept as a record only (not a *.test.ts file so CI does not run it).
 */
export const SUPERSEDED_BY =
  "test/sdd/speedrun-pg-finality.target.test.ts";
