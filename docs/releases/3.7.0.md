# Athena-JS 3.7.0 — Dragunov-compatible

| Field | Value |
| --- | --- |
| Package | `@xylex-group/athena` |
| Version | **3.7.0** |
| Server range | Athena **4.1.x** + Athena **5.0.x — Dragunov** |
| Breaking SDK major? | **No** |
| Spec | `docs/sdd/athena-js-3.7-dragunov-compatibility.md` |

## Highlights

- Root `query()` retained (deprecated alias).
- Preferred raw SQL: `admin.query({ sql, operation, expectedShape })`.
- Release identity: `health()` / `system.release()` / `system.compatibility()`.
- Route inventory mirrored from monorepo `athena-5-route-manifest.json`.

## Verification (local)

```bash
cd packages/athena-js
npx tsx --test test/dragunov-release-identity.test.ts test/query-builder-behavior.test.ts
npx tsc -p tsconfig.typecheck.json --noEmit
```

Result at program land: **122 pass**, typecheck clean.
