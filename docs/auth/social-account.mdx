# Auth Social and Provider Accounts

## Endpoints and methods

- `auth.social.link()` -> `POST /link-social`
- `auth.account.list()` -> `GET /list-accounts`
- `auth.account.unlink()` -> `POST /unlink-account`
- `auth.deleteUser.callback()` -> `GET /delete-user/callback`
- `auth.refreshToken()` -> `POST /refresh-token`
- `auth.getAccessToken()` -> `POST /get-access-token`
- `auth.health()` -> `GET /health`
- `auth.ok()` -> `GET /ok`
- `auth.error()` -> `GET /error`

## Examples

```ts
import { auth } from "./auth-client"

await auth.auth.social.link({
  provider: "google",
  callbackURL: "https://app.example.com/auth/callback",
})

await auth.auth.account.list()

await auth.auth.account.unlink({
  providerId: "google",
  accountId: "acc_123",
})

await auth.auth.deleteUser.callback({
  token: "delete_callback_token",
  callbackURL: "https://app.example.com/delete-callback",
})

await auth.auth.refreshToken({
  providerId: "google",
  accountId: "acc_123",
  userId: "usr_123",
})

await auth.auth.getAccessToken({
  providerId: "google",
  accountId: "acc_123",
  userId: "usr_123",
})

await auth.auth.health()
await auth.auth.ok()
await auth.auth.error()
```
