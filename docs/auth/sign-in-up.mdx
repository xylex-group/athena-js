# Auth Sign In and Sign Up

## Endpoints and methods

- `auth.signIn.social()` -> `POST /sign-in/social`
- `auth.signIn.email()` -> `POST /sign-in/email`
- `auth.signIn.username()` -> `POST /sign-in/username`
- `auth.signUp.email()` -> `POST /sign-up/email`

## Examples

```ts
import { auth } from "./auth-client"

await auth.auth.signIn.social({
  provider: "google",
  callbackURL: "https://app.example.com/auth/callback",
})

await auth.auth.signIn.email({
  email: "user@example.com",
  password: "password",
  rememberMe: true,
})

await auth.auth.signIn.username({
  username: "user1",
  password: "password",
  rememberMe: true,
})

await auth.auth.signUp.email({
  name: "New User",
  email: "new-user@example.com",
  password: "password",
  callbackURL: "https://app.example.com/onboarding",
})
```
