{
  "name": "AggregatePayments",
  "description": "A function to aggregate payment details like summing the amount over a time period. Options include finding the highest, lowest, average and sum of a set of payments.",
  "inputSchema": {
    "properties": {
      "profile_id": {
        "description": "The identifier referring to the profile this entity belongs to. This allows you to filter search results by a specific profile.",
        "type": "string"
      },
      "aggregation_function": {
        "description": "The aggregation function that should be used to calculate the result.",
        "enum": [
          "SUM",
          "MAX",
          "MIN"
        ],
        "type": "string"
      },
      "from_date": {
        "title": "From Date"
      },
      "to_date": {
        "title": "To Date"
      },
      "testmode": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "Most API credentials are specifically created for either live mode or test mode. In those cases the testmode query parameter can be omitted. For organization-level credentials such as OAuth access tokens, you can enable test mode by setting the testmode query parameter to true."
      }
    },
    "required": [
      "profile_id",
      "aggregation_function"
    ],
    "type": "object"
  }
}