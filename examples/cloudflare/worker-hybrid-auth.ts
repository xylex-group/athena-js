/**
 * Hybrid edge: local D1 + remote Athena Auth.
 *
 * Canonical single-concern example: 15-hybrid-auth.ts
 * This file re-exports that Worker so older docs/links keep working.
 */
export { default } from "./15-hybrid-auth.ts";
export type { ExampleEnv as Env } from "./shared/env.ts";
