/**
 * Public package entry for `@xylex-group/athena/social-providers`.
 *
 * Implementation lives under `src/auth/social-providers` (Better Auth–compatible
 * layout) and is re-exported here so consumers keep a stable import path.
 */
export * from "../auth/social-providers/index.ts";
